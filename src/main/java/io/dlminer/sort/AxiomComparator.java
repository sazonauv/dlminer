package io.dlminer.sort;

import java.util.Comparator;

import org.semanticweb.owlapi.model.OWLAxiom;

public abstract class AxiomComparator 
	extends AbstractComparator
	implements Comparator<OWLAxiom> {
	
}
